[core]
	editor = nvim
	pager = less
	excludesfile = ~/.gitignore

[alias]
	aa = add --all

	# BRANCHES
	br = !git --no-pager branch
	bv = !git --no-pager branch -v
	ba = branch -ra
	bd = branch -D
	bm = branch -m

	# COMMITS
	ca = commit --amend
	can = commit --no-edit --amend
	ci = commit -a -v
	cm = commit -am
	cmn = commit -amn

	# CHECKOUTS
	co = checkout
	cb = checkout -b

	di = diff
	dom = diff origin/master
	dum = diff upstream/master
	dh = rev-parse HEAD

	# LOG RELATED
	last = !git log -1 HEAD --pretty=format:\"%h\" | awk '{print $1}'
	head = !git branch --show-current | xargs git log master..
	ohead = !git branch --show-current | xargs git log origin/master..
	uhead = !git branch --show-current | xargs git log upstream/master..
	ll = log --pretty=format:"%C(yellow)%h%Cred%d\\ %Creset%s%C(cyan)\\ [%cn]" --decorate --numstat
	lg = log --graph --oneline --decorate

	ri = rebase -i
	rom = rebase master
	rum = rebase upstream/master
	#ro = rebase --onto
	rba = rebase --abort
	rbs = rebase --skip
	rbc = rebase --continue

	mf = merge --ff -v
	mm = merge --no-ff
	ms = merge --squash

	pl = pull -v
	plff = pull --ff-only
	st = status --short --branch
	tg = tag -a

	p = push
	pt = push --tags
	pf = push -f
	pm = push master

	po = push origin
	pof = push -f origin

	pu = push upstream
	pum = push upstream master
	pom = push origin master
	pomf = push -f origin master
	pon = push origin next
	ponf = push -f origin next

	un = reset --hard
	unom = reset --hard origin/master
	unum = reset --hard upstream/master
	unun = reset --hard upstream/next

	undo = checkout HEAD --
	unstage = reset HEAD --

	fu = fetch upstream
	fum = fetch upstream master
	fo = fetch origin
	fom = fetch origin master

	sl = stash list
	sp = stash pop
	ss = stash show -v
	sc = stash clear

	rs = remote show
	rso = remote show origin
	permission-reset = !git diff -p -R | grep -E \"^(diff|(old|new) mode)\" | git apply

[color]
	grep = auto
	diff = auto
	status = auto
	branch = auto

[branch]
	autosetuprebase = always
[http]
	sslverify = false
[push]
	default = current
[merge]
	tool = bc3
[mergetool]
	bc3 = trustExitCode
[diff]
	tool = bc3
[difftool]
	bc3 = trustExitCode
